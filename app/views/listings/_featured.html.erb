<div class="container">
    <h1 style="text-align:center">
      <%= link_to "Featured Listings", root_path %>
    </h1>
</div>
<% @listings.each do |listing| %>
  <div class="container">
    <div class="row">
      <%= form_with(model: listing, local: true) do |form| %>
      <div class="col">
        <div class="form-group row">
          <div class="col">
            <%= form.text_field :title, class:'form-control' %>
          </div>
          <div class="col-form-label">
            <%= form.label "Featured", class: 'col-form-label' %>
          </div>
          <div class="col"
            <%= form.check_box :featured, class:'form-control' %>
          </div>
          <div class="col">
            <%= form.submit "Update" %>
          </div>
        </div>
      </div>
      <% end %>
      <div class="col">
        <%= link_to "Delete", listing, method: :delete, data: {confirm: "Are you sure you want to delete the listing?"}, class:'btn btn-danger' %>
      </div>
    </div>
  </div>
<% end %>