<%= form_with(model: kind_of_boat, local: true) do |form| %>
  <% if kind_of_boat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kind_of_boat.errors.count, "error") %> prohibited this kind_of_boat from being saved:</h2>

      <ul>
        <% kind_of_boat.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :kind %>
    <%= form.text_field :kind %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
